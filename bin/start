#!/bin/env sh

# USAGE: gelli start - Start the service for faster use
#
# gelli start - Stat the service for faster responses

swapconf

llama-swap \
  --config "$SYS/swap.yaml" \
  --listen "0.0.0.0:${GELLI_PORT:-7771}" \
  --watch-config
